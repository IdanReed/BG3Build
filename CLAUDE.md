See @AGENTS.md.

## Git

Pushing `main` to origin, deleting merged branches (local and on origin), pruning
remotes, and dropping stashes are pre-authorized for this repository. Do them when
the task calls for them, without asking. Force-pushes still need confirmation. The
matching permission rules live in `.claude/settings.json`.
